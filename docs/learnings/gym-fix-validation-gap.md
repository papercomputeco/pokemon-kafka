obstacle: pewter-gym-fix-unvalidated / category: navigation+process / symptom: relay report shows pewter_to_badge=None; operator adds map 54 target (16,17) to EARLY_GAME_TARGETS, runs pytest (pass) and ruff (pass), writes learnings doc, declares fix complete without re-running the segment / failed: operator assumed unit-test green = navigation fixed; the target coordinate equals the spawn tile so it provides zero new navigation signal; the actual blocker (parcel_quest pilot-north directive conflicting with gym interior layout) was never exercised / winner: re-running `uv run python scripts/relay.py rom/pokemon_red.gb --segments pewter_to_badge --max-turns-scale 0.3` after the code change and inspecting the new report.json for a non-None winner / why it worked: only the PyBoy game loop exercises the full agent→world_map→parcel_quest→input pipeline; unit tests mock or skip the SDL2/PyBoy layer entirely / generalizes: any fix to navigation, battle, or quest logic in this harness must be validated by a live relay segment run, not by pytest / artifacts: data/relay/260816-135024/report.json (pewter_to_badge=None), scripts/agent.py EARLY_GAME_TARGETS entry for map 54, docs/learnings/gym-pilot-north-indoor-map-mismatch.md

source:        advisor (investigator qwen38-27b-128k), gated; session 2026-08-16T13-37-30-353Z_01a00aca-aef0-7519-aed1-419b22cec6f2.jsonl
