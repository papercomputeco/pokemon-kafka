obstacle:      viridian-forest-turn-385-blackout
category:      navigation | battle
symptom:       Lanes enter Viridian Forest (map 51) with ~L7-L9 starter and get stuck for thousands of turns; max_stuck_streak 45-145, stuck_count 500+, final_map_id remains 51 after 6000 turns. Base/cautious/aggressive/status_heavy/cautious_narrow all black out or thrash in place.
failed:        -
  variant: base (hp_run_threshold=0.25)
  failure: Fights too many wild battles; 134 encounters, 599 stuck events, lead_hp drops to 5. The forest maze with frequent encounters drains HP until the agent is too weak to navigate out.
  variant: cautious (hp_run_threshold=0.35)
  failure: Similar pattern — 181 encounters, 587 stuck events, ends at (25,21) in map 51 with 12 HP but still trapped.
  variant: aggressive (hp_run_threshold=0.1)
  failure: Fights even more aggressively; 62 encounters but 513 stuck events. Low flee threshold means it never escapes when surrounded.
  variant: status_heavy (status_move_score=5.0)
  failure: 128 encounters, 672 stuck events. Prioritizing status moves doesn't help in the forest where raw damage + survival is needed.
  variant: cautious_narrow (hp_run_threshold=0.35 + waypoint_skip_distance=1)
  failure: 118 encounters, 621 stuck events. Narrow waypoints don't compensate for the HP drain.
winner:        Stale-block unseal (2026-08-16, on top of the very_cautious genome hp_run_threshold=0.5 / hp_heal_threshold=0.5 and the battle-menu sync): `WorldMap.drop_stale_blocks` (scripts/world_map.py) retests hard-blocks that `observe` has since seen walkable, called from the map-51 branch of `choose_overworld_action` (scripts/agent.py) at the exact deadlock — exit not reachable even optimistically AND no frontier left to explore. Turns: 1062, lead_hp: 23, final_map_id: 2 (Pewter City), identical under PYTHONHASHSEED 0/1/7; the 1-HP entrance (`forest-crossing-1hp`) crosses too (XPASS 1393 / hp 22, all three seeds). The 2270-turn very_cautious genome PASS this file originally recorded was seed-7 luck — seeds 0/1 stalled at 3000 — so the genome was never the whole answer.
why it worked:  The forest has exactly one passage to the (1,0)/(2,0) north-gate warps: the x=1-2 corridor through (1,17)/(2,18). A bug catcher wanders that pocket, and a press into an NPC's tile fails exactly like a press into a wall — two consecutive fails hard-block the tile. Both choke tiles got blocked in the same run (their expiry counters read 14 and 19 "seen walkable again" — the NPC signature; real trees stay at 0), and blocks only expire after 25 in-view observations: once the planner had nothing left to chase in the northwest, the agent never returned, so the seal was permanent. From there `known_reachable` = False, the optimistic `require_reach` plan = None, `explore_step` = None (maze fully stamped), and the run fell to `cross_step`, whose candidate sweep picks a south candidate from (25,21) and a north candidate from (25,22) — the measured (25,21)-(25,22)/(26,21)-(26,22) two-cycle for the rest of the run (verified by replaying the wedge's saved WorldMap). Dropping the observation-contradicted blocks at the deadlock reopens the corridor; `known_reachable` flips true and the commit branch walks it out (UNSEAL fires twice per run: once mid-forest, once when the NPC re-blocks a corridor tile at (1,5)). A wrong drop just re-blocks after two failed presses — the retest can only un-wedge. Poison white-out to Pallet, seen on the old 3000-turn thrash runs, never happens now: the crossing finishes in ~1000 turns on the first pass, so no separate heal/Center policy was needed.
generalizes:   Hard-blocks learned from failed presses can be NPC shadows, and they only decay while watched — any map whose exit corridor is one or two tiles wide can be sealed forever by a wanderer. "Fully mapped + goal unreachable + nothing to explore" is not a navigation state, it is evidence that some block is stale: retest the blocks the world has since contradicted before falling back to boundary sweeps. Genome-level caution (hp_run_threshold/hp_heal_threshold at 0.5) still matters for surviving the grass corridor, but it cannot fix a sealed map.
artifacts:     data/relay/260815-190939/batons/forest_to_pewter.state, data/relay/260815-190939/report.json, data/relay/260815-190939/forest_to_pewter/*/agent.log
