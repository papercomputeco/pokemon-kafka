# Point the agent at the Tapes proxy (runs inside Docker Compose):
ANTHROPIC_API_BASE=http://localhost:8080

# The proxy forwards to the real Anthropic API by default.
# To override the upstream target, edit the --upstream flag in docker-compose.yml.
