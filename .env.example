# Tapes proxy runs inside Docker Compose and publishes to Kafka.
# Point the agent's API base to the proxy:
ANTHROPIC_API_BASE=http://localhost:8080

# Override the upstream LLM API if needed (default: https://api.anthropic.com):
# ANTHROPIC_API_BASE=https://api.anthropic.com
